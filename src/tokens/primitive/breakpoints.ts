/**
 * Kedama Design System — プリミティブ ブレイクポイントトークン
 *
 * 業務システム・SaaS を想定。
 * デスクトップ中心だがタブレット・モバイルでの利用も考慮する。
 *
 * Calm UI の方針:
 * - 画面が大きくなっても要素を詰め込みすぎない
 * - モバイルでは左右に 16〜24px の余裕を確保
 * - xl 以上ではコンテンツ幅を制限し、左右に広大なマージンを確保
 *   → ユーザーの視線を中央に集中させ、ストレスを軽減
 *
 * Figma Variables との対応:
 *   Collection: "Primitives" > Group: "Breakpoint"
 */

// ─── Breakpoints ────────────────────────────────────────
export const breakpoints = {
  /** 640px — モバイル。左右 16〜24px の余裕を持たせる */
  sm: '640px',
  /** 768px — タブレット */
  md: '768px',
  /** 1024px — デスクトップ */
  lg: '1024px',
  /** 1280px — ワイドデスクトップ */
  xl: '1280px',
} as const;

// ─── Content Width ──────────────────────────────────────
// 「デバイスのサイズ」と「情報の器」を分離して管理する。
// xl 以上の画面では maxWidth でコンテンツ幅を制限し、
// 左右に広大なマージン（余白）を確保する。
// コンテナは常に margin-left: auto; margin-right: auto で中央配置。
export const contentWidth = {
  /** 768px — 集中と没入。1行40〜60文字に収まり、読みやすさ最優先（記事・フォーム等） */
  narrow: '768px',
  /** 1200px — 安定とバランス。目だけで追える限界幅（LP・メインフィード・カード型） */
  default: '1200px',
  /** 1440px — 俯瞰と余裕。100%にしないことで情報密度をコントロール（ダッシュボード・管理画面） */
  wide: '1440px',
} as const;

// ─── Container Padding ──────────────────────────────────
// ブレイクポイントごとのコンテナ左右パディング。
// 画面が広がるにつれ余白を段階的に増やし、サイズ変化の衝撃を和らげる。
export const containerPadding = {
  /** モバイル — 左右 16px */
  sm: '16px',
  /** タブレット — 左右 24px */
  md: '24px',
  /** デスクトップ — 左右 32px */
  lg: '32px',
  /** ワイド — 左右 48px */
  xl: '48px',
} as const;
